
function dataTypeSearcher(){
  for(x in res){
   if(dataTypes.includes(x)){
    dataValue=dataGenerator(x);
    if(Array.isArray(res[x])){
     arrLength=res[x].Length;
     for(i=0;i<arrLength;i++){
      if( res[x].Name==parentTagName){

      }           
    }
  }
}       
}
}